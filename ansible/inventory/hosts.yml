all:
  vars:
    ansible_user: darthv
    ansible_ssh_private_key_file: ~/.ssh/id_rsa
    ansible_python_interpreter: /usr/bin/python3

  children:

    arch:
      hosts:
        arch01:
        arch02:
        arch03:


    vms:
      hosts:
        vm01:
        vm02:
        vm03:

    omv:
      hosts:
        openmediavault:
